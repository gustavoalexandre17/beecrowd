#include <iostream>
using namespace std;
const int limit = 20;

// valor maximo pra o primeiror elemento = n - ( k + 1 )

int main(int argc, char *argv[]) {
  int n, k;
  for (int i = 0; i < limit; i++) {
    cin >> n >> k;
  }
  return 0;
}
